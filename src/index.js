import React from 'react';
import ReactDOM from 'react-dom/client';
import PhaseOne from './views/PolyanetCross';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PhaseOne />
  </React.StrictMode>
);
